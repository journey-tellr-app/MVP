const express = require('express');
const pool = require('../../modules/pool');
const router = express.Router();

// STRETCH get invites for teams for use in notifications
router.get('/', (req, res) => {

});

//STRETCH store invites to team_person for use in notifications
router.post('/', (req, res) => {

});

//STRETCH and others

module.exports = router;